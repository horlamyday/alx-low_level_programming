#include <stdilb.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point 
 *
 * Return: always 0 (Success/correct)
 */

int main(void)
{
int n:

srand (time(0));
n = rand() - RANO_MAX / 2:

if (n ==0)
{
printf("%! is zero\n" , n);
}

else if (n<0)
pritf ("%! is negative\n" ,n);
}

else
{ 
printf ("%! is positive\n" , n);
}

return (0):
	
}
