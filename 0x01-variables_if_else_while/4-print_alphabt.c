#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entring point 
 *
 * Retrun: Always 0 (Success/correct)
 */

int main(void)

{
char alphabet;

for (alphabet = 'a' ; alphabet <= 'z' ; alphaabet ++)
{
if  (alphabet == 'q')
continue;

else if (alphabet == 'e')
continue;

putchar (alphabet)
}

putchar ('\n');

return (0);

}
